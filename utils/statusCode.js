const NOT_FOUND = 404;
const STATUS_OK = 200;
const TALKER_NOT_FOUND_MESSAGE = { message: 'Pessoa palestrante não encontrada' };

module.exports = {
  NOT_FOUND,
  STATUS_OK,
  TALKER_NOT_FOUND_MESSAGE,
};
